# ピタゴラスの定理

for a in 1..332 do
  for b in (a + 1)..499 do
    c = 1000 - a - b
    if a*a + b*b == c*c
      puts a,b,c
      break
    else
      b += 1
    end
  end
  break if a*a + b*b == c*c
  a += 1
end